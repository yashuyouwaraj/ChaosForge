global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "chaosforge-backend"
    scheme: "__PROMETHEUS_BACKEND_SCHEME__"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "__PROMETHEUS_BACKEND_TARGET__"
